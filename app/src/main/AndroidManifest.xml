<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.venter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AcceuilboutiqueActivity" />
        <activity
            android:name=".AcceuilActivity"
            android:label="@string/title_activity_acceuil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddProdActivity" />
        <activity android:name=".BoutiqueActivity" />
        <activity android:name=".ajout_produitActivity" />
        <activity android:name=".InscriptionActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>